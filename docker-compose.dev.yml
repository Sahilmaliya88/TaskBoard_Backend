version: '3'
services:
  nginx:
    ports:
      - "3000:80"
  node-app:
    env_file:
      - ./.env.local
  mongo:
    image: mongo:latest
    volumes:
      - mongo-db:/data/db
    environment:
      - MONGO_INITDB_ROOT_USERNAME=Sahil
      - MONGO_INITDB_ROOT_PASSWORD=root
volumes:
  mongo-db: